/** Export/Import for FileExport/FileImport. */
const EXPORTS = {
  /** Export name to use for default exports/imports. */
  DEFAULT: 'default',
  /** Export name to use for namespace exports/imports. */
  NAMESPACE: '*',
} as const;

export { EXPORTS };
